#include<stdio.h>
void main()
{
    long int n;
    scanf("%d",&n);
    int x,y,r,s=0;
    scanf("%d%d",&x,&y);
    while(n>0)
    {
        r=n%10;
        if(r==x)
            x=y;
        s=s*10+r;
        n/10;
    }
}
